#!/bin/bash
#PBS -N strong_mpi
#PBS -q short_cpuQ
#PBS -l select=4:ncpus=96:mpiprocs=32
#PBS -l walltime=06:00:00
#PBS -j oe
#PBS -o results/strong_mpi.log

set -euo pipefail
cd "$PBS_O_WORKDIR"

module purge
module load gcc91
module load mpich-3.2.1--gcc-9.1.0

mkdir -p results

: "${MATRIX:?Use qsub -v MATRIX=matrix/.../...mtx}"
: "${P_LIST:=1 2 4 8 16 32 64 128}"
: "${ITERS:=50}"
: "${WARMUP:=3}"
: "${RUNS:=10}"
: "${SEED:=1}"

TAG="$(basename "$(dirname "$MATRIX")")"
OUTCSV="results/strong_${TAG}_mpi.csv"

mpicxx -O3 -std=c++17 -fopenmp -march=native -o src/smpv_d2 src/smpv_d2.cpp
[ -x src/smpv_d2 ] || { echo "Build failed"; exit 11; }

export OMP_NUM_THREADS=1
export OMP_PROC_BIND=close
export OMP_PLACES=cores

rm -f "$OUTCSV"

for P in $P_LIST; do
  mpirun -np "$P" ./src/smpv_d2 \
    --case strong \
    --matrix "$MATRIX" \
    --seed "$SEED" \
    --iters "$ITERS" \
    --warmup "$WARMUP" \
    --runs "$RUNS" \
    --no-omp \
    --out "$OUTCSV"
done
